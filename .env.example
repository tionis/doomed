VITE_INSTANT_APP_ID=

# Optional defaults for the host-side AI settings panel.
# Tokens are never stored in InstantDB. They stay in the host browser.
VITE_DEFAULT_AI_PROVIDER=mock
VITE_DEFAULT_AI_BASE_URL=
VITE_DEFAULT_AI_MODEL=mock
VITE_DEFAULT_AI_RESPONSE_FORMAT=json_object
